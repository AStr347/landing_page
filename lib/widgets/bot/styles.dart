import 'package:flutter/material.dart';

const backgroundColor = Color.fromRGBO(207, 216, 220, 1);
const textColor = Color.fromARGB(255, 0, 0, 0);

const botTextStyle = TextStyle(color: textColor, fontFamily: "Tahoma", fontSize: 14);
